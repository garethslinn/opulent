export interface IndustryData {
    industry: string;
    companies: string[];
}

export interface IndustryData {
    industry: string;
    companies: string[];
}

export interface BarGraphProps {
    data: IndustryData[];
    subtext: string;
}
